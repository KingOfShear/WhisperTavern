{
  "name": "@whispertavern/agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Agent Runtime:workflow / tools / skills / memory / artifacts(P3 落地)",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
